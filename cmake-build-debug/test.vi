A = {};
counter = 0;

function to(x) : {
    global A; global counter;
    if (counter == x) : {
        return A;
    } else : {
        counter = counter + 1;
        A[] = counter;
        return to(x);
    }
}

to(10);
A;