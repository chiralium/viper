global_var = 3.14;

namespace A : {
    output("Namespace declaration: " + __name__ + "\n");
    output("This namespace are extending the global namespace data. For example built-in function `output`\n");
    output("Even global_var, that stored in global namespace: " + string(global_var) + "\n");
}